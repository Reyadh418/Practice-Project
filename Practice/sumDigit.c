#include <stdio.h>

int main ()
{
    // Declare variables
    int n, sum = 0, last_digit;

    //User number input
    printf("Enter the number(Integer only):\t");
    scanf("%d", &n);
    printf("Your entered number %d", n);

    //The Process
    
}